<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>rss news</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,700" rel="stylesheet" />
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <main>
      <div class="cards">


        <% for (let i = 0; i < news.length; i++) { %>


        <div class="card">
          <div class="title">
            <h2><%= news[i].title %></h2>
          </div>
          <ul>
            <% news[i].items.forEach((item) => { %>
            <li><a href="<%= item.link %>" target="_blank"><%= item.title %></a></li>
            <% }); %>
          </ul>
        </div>

        <% } %>

    </div>
    </main>
    <div class="footer">
      <p>from <a href="https://github.com/semiformal-net/rssboxes" target="_blank">semiformal</a></p>
    </div>
  </body>
</html>
